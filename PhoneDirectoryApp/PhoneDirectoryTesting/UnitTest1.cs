using System;
using PhoneDirectoryLibrary; 
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace PhoneDirectoryTesting
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            /*
             *
             *Test Main by READING a directory 
             * CREATING a object from "Example.txt"
             * ADDING the object
             * SEARCING for object
             * DELETING saif object. 
             * 
             * 
             */
        }
    }
}
